# Podcast Copilot - Python dependencies
# Install with: pip install -r requirements.txt

# Mac menu bar
rumps>=0.4.0

# Audio capture & processing
sounddevice>=0.4.6
numpy>=1.24.0

# System audio capture uses a native Swift dylib (no pyobjc needed)
# Build: cd audio && swiftc -O -emit-library -target x86_64-apple-macosx14.0 -framework ScreenCaptureKit -framework CoreMedia -module-name AudioCaptureHelper -o libaudio_capture.dylib capture_helper.swift

# OpenAI API (Whisper + GPT-4o)
# NOTE: Only called when you say "explain that" — never during passive listening
openai>=1.0.0

# Recommended: Porcupine on-device wake word detection
# pip install pvporcupine
# Get free key at https://console.picovoice.ai/
pvporcupine>=3.0.0
