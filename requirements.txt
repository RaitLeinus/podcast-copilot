# Podcast Copilot - Python dependencies
# Install with: pip install -r requirements.txt

# Mac menu bar
rumps>=0.4.0

# Audio capture & processing
sounddevice>=0.4.6
numpy>=1.24.0

# System audio capture uses a native Swift helper (no pyobjc needed)
# Build with: swiftc -O -framework ScreenCaptureKit -framework CoreMedia -o audio_capture_helper audio_capture_helper.swift

# OpenAI API (Whisper + GPT-4o)
# NOTE: Only called when you say "explain that" — never during passive listening
openai>=1.0.0

# Recommended: Porcupine on-device wake word detection
# pip install pvporcupine
# Get free key at https://console.picovoice.ai/
pvporcupine>=3.0.0
